(* using this file should be equivalent to "make run-isap" *)
cd "isa_src";
PolyML.use "ROOT.ML";
cd "..";

cd "isap_src";
PolyML.use "ROOT.ML";
cd "..";

cd "parser";
PolyML.use "ROOT.ML";
cd "..";

